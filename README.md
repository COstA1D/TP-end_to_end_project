# TP-End_To_End_Project

Учебный сквозной data-проект с поэтапной разработкой по неделям.

## Структура проекта

- `src/` — исходный код проекта
- `data/` — данные проекта
- `notebooks/` — исследовательские ноутбуки
- `docs/` — документация
- `configs/` — конфигурационные файлы
- `tests/` — тесты
- `scripts/` — служебные скрипты

## Запуск на Windows

1. Установить Anaconda или Miniconda.
2. Открыть **Anaconda Prompt**.
3. Перейти в папку проекта:
   ```bat
   cd C:\Users\***\OneDrive\Desktop\е\end-to-end-project
   ```
4. Выполнить команду:
   ```bat
   scripts\setup_env.bat
   ```

Если в обычном терминале команда `conda` недоступна, запускать скрипт нужно именно через **Anaconda Prompt**.

## Smoke test

Скрипт `scripts\setup_env.bat` автоматически:

- находит `conda`;
- создаёт окружение `tp_project_env`, если оно ещё не существует;
- устанавливает зависимости из `requirements.txt`;
- запускает `broken_env.py`.

Успешный результат:

- выводится путь к активному интерпретатору Python;
- выводится версия установленного `pandas`;
- в конце появляется строка:
  ```text
  [OK] Environment is ready
  ```

## Статус Week 2

Для второй недели подготовлена шаблонная версия слоя Extract:

- чтение конфигурации из YAML;
- HTTP GET-запрос с `timeout`;
- обработка сетевых, HTTP- и JSON-ошибок;
- сохранение raw JSON в `data/raw/...`.

Финальная конфигурация API для семестрового варианта будет добавлена после получения официального файла `variant_XX.yml` из учебных материалов.

## Прогресс по неделям

- [x] Week 1 — настройка Conda, структура репозитория, smoke test
- [x] Week 2 — HTTP/API и сохранение raw JSON
   ## Результат Week 2

   - Добавлена диагностика HTTP-запросов.
   - Реализован timeout.
   - Добавлена обработка сетевых ошибок.
   - Добавлена проверка HTTP-статуса.
   - Добавлена безопасная обработка JSON.
   - Реализован Extract для Open-Meteo.
   - Raw JSON сохранён в `data/raw/variant_02/`.
- [x] Week 3 — очистка и анализ погодных данных

В рамках Week 3 выполнена обработка почасовых погодных данных.

### Выполненные шаги

- Загружен JSON-файл с погодными наблюдениями.
- Данные преобразованы в таблицу pandas DataFrame.
- Поле `time` преобразовано в формат datetime.
- Проверены пропуски и дубликаты.
- Получено 168 почасовых наблюдений за период с 15 по 21 сентября 2026 года.
- Создана дневная сводка по температуре, влажности, осадкам и скорости ветра.
- Построен график почасовой температуры.

### Результаты

- Пропуски: отсутствуют.
- Дубликаты строк: отсутствуют.
- Дубликаты времени: отсутствуют.
- Самый тёплый день: 17 сентября 2026 года.
- Самый дождливый день: 17 сентября 2026 года.
- Максимальная скорость ветра: 23.7, 19 сентября 2026 года.

### Файлы

- `notebooks/week3_eda.ipynb` — ноутбук анализа.
- `data/processed/weather_hourly_normalized.csv` — очищенные почасовые данные.
- `data/processed/weather_daily_summary.csv` — дневная сводка.
- `data/processed/figures/temperature_hourly.png` — график температуры.
- [x] Week 4 — группировки, join и витрина

### Выполнено

- Продемонстрирован опасный many-to-many join.
- Показано увеличение количества строк с 3 до 5.
- Реализован безопасный many-to-one join.
- Добавлена проверка кардинальности через `validate="many_to_one"`.
- Построена дневная погодная витрина.
- Рассчитаны KPI температуры, осадков, влажности и ветра.
- Добавлены 3-дневные rolling-метрики.

### Слои данных

- `raw` — исходный ответ API.
- `normalized` — плоская очищенная таблица почасовых наблюдений.
- `mart` — агрегированная дневная витрина для анализа и визуализации.

Разделение слоёв позволяет отдельно хранить исходные данные, очищенный табличный слой и готовые аналитические показатели. Это упрощает повторную обработку, проверку качества и построение дашбордов.
- [x] Week 5  — SQL и PostgreSQL

- PostgreSQL установлен локально.
- Создана база `weather_db`.
- В таблицу `public.mart_variant_02_daily_weather` загружено 7 строк.
- Загрузка реализована в `src/load.py`.
- Используется транзакция `engine.begin()`.
- Повторная загрузка не создаёт дубликаты.
- SQL-проверки находятся в `docs/sql_checks.md`.
- [x] Week 6 - добавлен ETL pipeline с full incremental и state
- [ ] Week 7
- [ ] Week 8
- [ ] Week 9
- [ ] Week 10
- [ ] Week 11
- [ ] Week 12
- [ ] Week 13
- [ ] Week 14
